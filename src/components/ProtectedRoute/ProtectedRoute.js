// import { Navigate } from "react-router-dom";

// const ProtectedRoute = ({ children }) => {
//   const isLoggedIn = /* Kiểm tra trạng thái đăng nhập (ví dụ, từ context hoặc localStorage) */;

//   if (!isLoggedIn) {
//     // Nếu chưa đăng nhập, chuyển hướng đến trang login
//     return <Navigate to="/login" />;
//   }

//   return children; // Hiển thị nội dung của route nếu người dùng đã đăng nhập
// };